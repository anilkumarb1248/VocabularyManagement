import { WordSearchRequest } from './word-search-request';

describe('WordSearchRequest', () => {
  it('should create an instance', () => {
    expect(new WordSearchRequest()).toBeTruthy();
  });
});
